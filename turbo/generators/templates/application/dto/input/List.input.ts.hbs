/**
 * @fileoverview List {{pascalCase context}}s Input DTO
 */

import { z } from 'zod'

export const List{{pascalCase context}}sSchema = z.object({
	page: z.number().int().min(1).default(1),
	limit: z.number().int().min(1).max(100).default(20),
	sortBy: z.enum(['createdAt', 'updatedAt']).optional(),
	sortOrder: z.enum(['asc', 'desc']).optional(),
	status: z.string().optional(),
	search: z.string().optional(),
})

export type List{{pascalCase context}}sInput = z.infer<typeof List{{pascalCase context}}sSchema>
